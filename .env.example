# 🔐 SECURITY NOTICE - READ THIS FIRST! 🔐
#
# This file contains configuration for the Car Audio Events platform.
# CRITICAL SECURITY REQUIREMENTS:
#
# 1. NEVER commit .env files to version control
# 2. SERVICE ROLE KEYS must NEVER have VITE_ prefix (client exposure risk)
# 3. Use environment-specific secrets management in production
# 4. Rotate all keys regularly (see scripts/rotate-keys.ts)
# 5. Monitor for accidental key exposure (see scripts/scan-for-secrets.ts)
#
# Copy this file to .env and fill in your values
# DO NOT commit the .env file to version control

# ========================================================================
# PUBLIC CONFIGURATION (Safe for client-side use with VITE_ prefix)
# ========================================================================

# Supabase Configuration (PUBLIC)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Maps API (PUBLIC)
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# hCaptcha (PUBLIC)
VITE_HCAPTCHA_SITE_KEY=your_hcaptcha_site_key

# TinyMCE Editor API Key (PUBLIC)
# Get your free API key from: https://www.tiny.cloud/
VITE_TINYMCE_API_KEY=your_tinymce_api_key

# Payment Providers - PUBLISHABLE Keys Only (PUBLIC)
VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
VITE_PAYPAL_CLIENT_ID=your_paypal_client_id
VITE_PAYPAL_ENVIRONMENT=sandbox

# ========================================================================
# PRIVATE/SECRET CONFIGURATION (SERVER-SIDE ONLY - NO VITE_ PREFIX)
# ========================================================================
# 
# ⚠️ CRITICAL WARNING: These keys provide administrative access
# ⚠️ NEVER use VITE_ prefix for service role keys
# ⚠️ These should ONLY be used in Edge Functions and server-side code
# ⚠️ In production, use proper secret management (Vault, Azure Key Vault, etc.)
#

# Supabase Admin Access (SERVER-SIDE ONLY)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Payment Provider Secrets (SERVER-SIDE ONLY)
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret
PAYPAL_CLIENT_SECRET=your_paypal_client_secret

# Email Service (SERVER-SIDE ONLY)
MAILGUN_API_KEY=your_mailgun_api_key
MAILGUN_DOMAIN=your_mailgun_domain

# ========================================================================
# VAULT CONFIGURATION (for production key management)
# ========================================================================

# Uncomment and configure for production vault integration
# VAULT_PROVIDER=hashicorp|azure|aws|local
# VAULT_ENDPOINT=https://vault.example.com
# VAULT_TOKEN=your_vault_token
# VAULT_NAMESPACE=your_vault_namespace
# VAULT_MOUNT=secret

# ========================================================================
# KEY ROTATION CONFIGURATION
# ========================================================================

# Admin ID for automated key rotation
# ROTATION_ADMIN_ID=system-automation

# Notification endpoint for rotation alerts
# ROTATION_NOTIFICATION_ENDPOINT=https://alerts.example.com/webhooks/key-rotation

# ========================================================================
# DEVELOPMENT/TESTING OVERRIDES
# ========================================================================

# Override for testing (development only)
# NODE_ENV=development